[package]
name = "dataplane-mat-runtime"
edition.workspace = true
license.workspace = true
publish.workspace = true
version.workspace = true

[features]
default = []

[dependencies]
cascade = { workspace = true, features = [] }
concurrency = { workspace = true, features = [] }
mat = { workspace = true, features = [] }

[dev-dependencies]
mat-state-sync = { workspace = true, features = [] }
