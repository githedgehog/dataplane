[workspace]

members = [
	"dpdk",
	"dpdk-sys",
	"errno", "fake-kani", "fake-kani-macro",
	"net",
	"scratch",
]
resolver = "2"

[workspace.dependencies]

bindgen = { version = "0.71.1" }
bolero = { version = "0.12.0" }
criterion = { version = "0.5.1", default-features = true }
doxygen-rs = { version = "0.4.0" }
etherparse = { version = "0.16.0", default-features = false, features = [] }
rand = { version = "0.8.5", default-features = false, features = [] }
serde = { version = "1.0.213", default-features = false, features = ["derive", "alloc", "rc"] }
thiserror = { version = "2.0.4", default-features = false }
tracing = { version = "0.1.40", default-features = false, features = ["attributes"] }
tracing-subscriber = { version = "0.3.18" }
tracing-test = { version = "0.2.5" }
no-panic = { version = "0.1.33", default-features = false }

[profile.release]
debug = "full"
codegen-units = 1
lto = "thin"
opt-level = 3
incremental = false

[profile.fuzz]
inherits = "release"
