[package]
name = "dataplane-nat"
edition.workspace = true
license.workspace = true
publish.workspace = true
version.workspace = true

[features]
loom = ["concurrency/loom"]
shuttle = ["concurrency/shuttle", "dep:shuttle"]
shuttle_dfs = ["concurrency/shuttle_dfs", "shuttle"]

[dependencies]
ahash = { workspace = true, features = ["std"] }
arc-swap = { workspace = true }
bnum = { workspace = true }
common = { workspace = true }
concurrency = { workspace = true, features = [] }
config = { workspace = true }
flow-entry = { workspace = true }
indenter = { workspace = true }
left-right = { workspace = true }
linkme = { workspace = true }
lpm = { workspace = true }
net = { workspace = true }
pipeline = { workspace = true }
rand = { workspace = true, features = ["thread_rng"] }
roaring = { workspace = true }
strum = { workspace = true }
thiserror = { workspace = true }
tracectl = { workspace = true }
tracing = { workspace = true }

[target.'cfg(not(miri))'.dependencies]
shuttle = { workspace = true, optional = true }

[dev-dependencies]
# internal
config = { workspace = true }
fixin = { workspace = true }
test-utils = { workspace = true }
lpm = { workspace = true, features = ["testing"] }
net = { workspace = true, features = ["bolero"] }
tokio = { workspace = true, features = ["macros", "rt", "time"] }
tracectl = { workspace = true }

# external
bolero = { workspace = true, default-features = false, features = ["alloc"] }
etherparse = { workspace = true }
tracing-test = { workspace = true, features = [] }

[target.'cfg(not(miri))'.dev-dependencies]
shuttle = { workspace = true, features = [] }

# Dependencies below pull in dpdk-sys, whose build cannot run on the miri cross target. Only
# #[dpdk::with_eal] tests use them, and those are gated with #[cfg(not(miri))], so keep the deps out
# of the miri dependency graph entirely.
dpdk = { workspace = true, features = ["test"] }
flow-filter = { workspace = true }
